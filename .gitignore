# private build docs — never commit
EXECUTION-CHECKLIST.md
*-checklist.md
*-CHECKLIST.md
RUNBOOK*.md
runbook*.md
NOTES*.md
notes/
TODO*.md

# secrets — never commit (commit .env.example only)
.env
.env.*
!.env.example

# forge dependencies ship as git submodules (pinned commits) — clone with --recursive,
# or run `forge install` inside contracts/.

# deps / build artefacts
node_modules/
worker/node_modules/
web/node_modules/
out/
artifacts/
cache/
broadcast/
forge-cache/
typechain-types/
coverage/
dist/
build/
.next/
web/.next/
web/out/
*.tsbuildinfo
next-env.d.ts

# logs / local state
*.log
logs/
tmp/
demo/local/state/
demo/local/*.json
.DS_Store
Thumbs.db
